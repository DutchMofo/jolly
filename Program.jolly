
int main()
{
	AnalyseTest a = {
		id: 1,
		count: 10,
		number: 12,
	};
	
	f64 test = 46;
	
	swapCountNumber(&a);
	return test;
}

AnalyseTest* swapCountNumber(AnalyseTest* b)
{
	b.(count, number) = b.(number, count);
	return b;
}

struct StructID
{
	int id;
}

struct AnalyseTest : StructID
{
	int count;
	int number;
}