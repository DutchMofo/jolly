struct Slice<$type> // #ordered // TODO: Implement array ordering
{
    int count;
    $type* data;
}

// $T getVec<$T>($T val)
// {
//     return val;
// }

int main()
{
	int* data = null;
	int[:] slice = data[0 : 10];
	
	
    // auto test = getVec(0.1);
    // return (i32: test);
	return 0;
}


// TODO:
// Disallow: i32 a, 10 * 2;

// Ending on a comment breaks the tokenizer

