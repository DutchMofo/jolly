
enum ID
{
	ANALYSE_TEST,
	OTHER,
}

struct StructID
{
	ID id;
}

struct AnalyseTest : StructID
{
	int count;
	int number;
}

int main()
{
	AnalyseTest a = {
		id: ID.OTHER,
		count: 10,
		number: 12,
	};
	
	
	int[] data;
	
	swapCountNumber(&a);
	return (i32: data.count);
}

AnalyseTest* swapCountNumber(AnalyseTest* b)
{
	b.(count, number) = b.(number, count);
	return b;
}