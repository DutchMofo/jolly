
int main()
{
	AnalyseTest a;
	
	a.count = 10;
	a.number = 1;
	swapCountNumber(&a);
	
	
	return 0;
}

bool, AnalyseTest* swapCountNumber(AnalyseTest* a)
{
	a.(count, number) = a.(number, count);
	return true, a;
}

struct AnalyseTest
{
	int count;
	int number;
}