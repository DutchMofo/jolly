struct Slice<$type, int $test> // #ordered // TODO: Implement array ordering
{
	int count;
	$type* data;
}

($T, $T, $T) getVec<$T>($T val)
{
	return (val, val, val);
}

(int, int) getVec() { return (0, 0); }
(int, int, int) getVec(int i) { return (i, i, i); }

int main() {
	auto test = getVec();
	return test.1;
}


// TODO:
// Disallow: i32 a, 10 * 2;

// Ending on a comment breaks the tokenizer

