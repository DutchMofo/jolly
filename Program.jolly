

(int, int, int) getVec3() {
	return (0, 0, 0);
}

namespace Jolly {
	(int, int, int) getVec3(int number) {
		return (number, number, number);
	}
	
	int main() {
		auto test = getVec3();
		return test.2;
	}
}

// TODO:
// Disallow: i32 a, 10 * 2;

// Ending on a comment breaks the tokenizer
