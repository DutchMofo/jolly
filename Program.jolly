
bool, AnalyseTest* swapCountNumber(AnalyseTest* a)
{
	a.(count, number) = a.(number, count);
	return true, a;
}

int main()
{
	AnalyseTest a;
	a.count = 10;
	a.number = 1;
	swapCountNumber(&a);
	
	return a[1:2];
}

struct AnalyseTest
{
	int count;
	int number;
}