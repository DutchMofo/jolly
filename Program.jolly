

// TODO: Fix: AnalyseTest* function();

AnalyseTest*, bool swapCountNumber()
{
	a.(count, number) = a.(number, count);
	return true, a;
}

int main()
{
	AnalyseTest a;
	int[] lala;
	
	a.count = 10;
	a.number = 1;
	swapCountNumber(&a);
	
	return lala[1:2];
}

struct AnalyseTest
{
	int count;
	int number;
}