struct Slice<$type, int $test> // #ordered // TODO: Implement array ordering
{
    int count;
    $type* data;
}

// ($T, $T, $T) getVec<$T>($T val)
// {
//     return (val, val, val);
// }

void getVec() {}
f32 getVec(f32 f) { return 0.1; }

int main()
{
    auto test = getVec(0.1);
    return (i32: test);
}


// TODO:
// Disallow: i32 a, 10 * 2;

// Ending on a comment breaks the tokenizer

